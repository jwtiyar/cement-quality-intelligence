# Local Rawmix Reference Files

This directory is reserved for local rawmix reference files used during
offline development. The reference files are intentionally not redistributed
with the repository.

Place local PDFs, spreadsheets, Word documents, and helper files here when
using the rawmix tools. Git ignores this directory so those files remain
available locally without being published.
